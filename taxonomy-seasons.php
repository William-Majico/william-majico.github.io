<?php get_header();$term_id  = get_queried_object_id();$term     = get_term_by('id', $term_id, 'seasons');$slug     = $term->slug;$data     = array('term' => $term, 'slug' => $slug, 'term_id' => $term_id); ?><div class="Body">    <div class="Main Container">        <?php do_action( 'season_content', $data );                #10: Carousel letter                #20: Info                #30: Related ?>    </div></div><?php get_footer(); ?>